# cli — headless edge daemon ("driver box"). Installs into the shared root .venv.
#
# Decision core (signals/trust/gating/escalation/fusion) is pure-Python and needs
# none of the heavy CV deps — those are only for the phone/replay camera path.

# signing + config (always needed)
pynacl>=1.5
python-dotenv>=1.0

# uplink + phone sensor stream
websocket-client>=1.7

# perception / camera path (phone + replay); heavy — skip for pure --sim runs
mediapipe>=0.10
opencv-python>=4.9
numpy>=1.26

# in-cabin audio (L5); degrades gracefully if the backend/libs are unavailable
simpleaudio>=1.0

# in-cabin driver panel (--panel); degrades to plain logs if absent / no TTY
rich>=13.0
