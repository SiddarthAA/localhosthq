# Shared Python venv (backend + cli) — mirror of the sensor-app convention
.venv/
__pycache__/
*.pyc

# Node
node_modules/

# Secrets & local config
.env

# Edge daemon: private signing key + downloaded model weights
cli/keys/
cli/models/
*.key

# Local SQLite files (edge outbox + any legacy ledger)
*.db
*.db-journal
*.db-wal
*.db-shm

.DS_Store
